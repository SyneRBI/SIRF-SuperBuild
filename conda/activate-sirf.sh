# Runtime env (conda-adapted from env_sirf.sh.in): SIRF_INSTALL_PATH for
# examples_data_path(), GADGETRON_HOME for the Gadgetron relay config.
export SIRF_INSTALL_PATH="${CONDA_PREFIX}"
export GADGETRON_HOME="${CONDA_PREFIX}"
