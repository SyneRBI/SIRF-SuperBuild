version: '3.2'
services:
 sirf:
  container_name: sirf
  image: ccppetmr/sirf:latest
  build:
   context: .
   # depends_on:
   #  - core
   cache_from:
    - ccppetmr/sirf:core
    - ccppetmr/sirf:latest
  stdin_open: true
  tty: true
  cap_add:
   - NET_ADMIN
  network_mode: "bridge"
  volumes:
   - ./devel:/devel
 core:
  image: ccppetmr/sirf:core
  build:
   context: .
   dockerfile: Dockerfile-core
   args:
    mainUser: sirfuser
   cache_from:
    - ubuntu:16.04
    - ccppetmr/sirf:core
